# Leave blank for local dev — Vite's dev server proxy (vite.config.js) handles /api.
# Set this to your deployed backend URL when building for production, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
